#!/usr/bin/env bash
# Double-click this file in Finder to start camera time sync (no terminal typing).
# It launches the host and opens the control UI in your browser.
cd "$(dirname "$0")"
exec ./run.sh
